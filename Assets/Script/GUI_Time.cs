using UnityEngine;
using System.Collections;

public class GUI_Time: MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		/*
		if (Controller.isTitle==true){
			guiText.text = "" ;
		}
		
		else if(Controller.gameStart==true && Controller.isTitle==false){ 
			guiText.text = "" + Controller.time ;
			if (Controller.time<=0){
				Controller.level += 1 ;
				Controller.time = Controller.level * 5 ;
				if (Controller.speed>= Controller.minSpeed){
					Controller.speed -= 0.1f ;
				}
				if (Controller.speed< Controller.minSpeed){
					Controller.speed = Controller.minSpeed ;
				}
				
			}
			else{
				Controller.time -= Time.deltaTime ;
			}
		}
		else{
			guiText.text = "\nGame Over\nPress Space to Restart";
		}
		*/
	}
}
